# ==============================================
# RENDER.COM PRODUCTION ENVIRONMENT VARIABLES
# ==============================================
# Copy these values into Render Environment tab
# Go to: Your Service > Environment > Add Environment Variable

# ============= REQUIRED - SET THESE FIRST =============

NODE_ENV=production
PORT=10000
JWT_SECRET=opengovdatahub-super-secure-jwt-2024-change-this-now
FRONTEND_URL=https://opengovdatahub.com

# ============= API & CACHE SETTINGS =============

EXTERNAL_API_TIMEOUT=30000
EXTERNAL_API_RATE_LIMIT=100
CACHE_TTL=3600
CACHE_MAX_SIZE=1000
CACHE_REFRESH_INTERVAL=300

# ============= EMAIL CONFIGURATION =============
# Configure these later when you set up Gmail app password
# For now, leave EMAIL_PASS empty - backend will still work

EMAIL_SERVICE=gmail
EMAIL_USER=hello@opengovdatahub.com
EMAIL_PASS=
FROM_NAME=OpenGov DataHub
FROM_EMAIL=hello@opengovdatahub.com

# ============= GOOGLE OAUTH (OPTIONAL) =============
# Add these if you want Google login later

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://opengovdatahub.com/auth/google/callback

# ==============================================
# INSTRUCTIONS:
# 1. Create your Render Web Service first
# 2. Go to Environment tab in Render dashboard  
# 3. Add each variable above (name=value)
# 4. Start with the REQUIRED section first
# 5. Add email config later when ready
# ==============================================